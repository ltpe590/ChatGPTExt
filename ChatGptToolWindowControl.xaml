<UserControl x:Class="ChatGptVsix.ChatGptToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="350" MinHeight="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Model:" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <ComboBox x:Name="ModelBox" Width="220" SelectedIndex="0">
                <ComboBoxItem Content="gpt-5"/>
                <ComboBoxItem Content="gpt-4o"/>
            </ComboBox>
            <TextBlock Text="  Max tokens:" VerticalAlignment="Center" Margin="0,0,2,0"/>
            <TextBox x:Name="MaxTokensBox" Width="30" Text="400"/>
        </StackPanel>

        <TextBox x:Name="PromptBox" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,8">
            <Button x:Name="SendBtn" Content="Send" Width="100" Click="SendBtn_Click" />
            <Button x:Name="CancelBtn" Content="Cancel" Width="100" Click="CancelBtn_Click" IsEnabled="False" />
            <TextBlock x:Name="StatusText" Margin="10,4,0,0" />
        </StackPanel>

        <TextBox x:Name="ResponseBox" Grid.Row="3" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" IsReadOnly="True"/>
    </Grid>
</UserControl>
